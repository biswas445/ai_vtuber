# ---------------------------------------------------------------------------
# "Evil" — realtime voice assistant (mic -> VAD -> STT -> LLM -> TTS -> VRM)
# Exact pin of every dependency the project was last known to work with.
#
# One-command restore:
#   python -m venv venv
#   venv\Scripts\pip install -r requirements.txt
#   venv\Scripts\pip install -e .
#
# Notes:
# * torch/torchaudio/torchvision are CUDA 12.6 builds: the inline
#   --extra-index-url below makes pip find the +cu126 wheels. If pip ever
#   refuses, install them in a second pass:
#     pip install --extra-index-url https://download.pytorch.org/whl/cu126 torch==2.13.0+cu126 torchaudio==2.11.0+cu126 torchvision==0.28.0+cu126
# * `dotenv` (the old, abandoned package) is deliberately NOT listed: it
#   shadows the same import name as python-dotenv. Only python-dotenv is kept.
# ---------------------------------------------------------------------------

--extra-index-url https://download.pytorch.org/whl/cu126

# --- voice pipeline: STT / VAD / audio I/O ---------------------------------
ctranslate2==4.8.1
faster-whisper==1.2.1
onnxruntime==1.29.0
silero-vad==6.2.1
sounddevice==0.5.6
soundfile==0.13.1
numpy==2.5.2
scipy==1.18.1

# --- LLM (Groq API) ---------------------------------------------------------
groq==1.6.0
python-dotenv==1.2.3

# --- CUDA PyTorch stack -----------------------------------------------------
torch==2.13.0+cu126
torchaudio==2.11.0+cu126
torchvision==0.28.0+cu126

# --- exact frozen versions of the remaining dependencies ---------------------
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
av==18.1.0
beartype==0.22.9
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.4.2
colorama==0.4.6
distro==1.9.0
einops==0.8.2
fastapi==0.141.1
filelock==3.32.4
flatbuffers==25.12.19
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.28.0
idna==3.19
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
packaging==26.3
pillow==12.3.0
protobuf==7.36.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.21.0
python-multipart==0.0.32
PyYAML==6.0.3
requests==2.34.2
rich==15.0.0
safetensors==0.8.0
sentencepiece==0.2.2
setuptools==84.0.0
shellingham==1.5.4
sniffio==1.3.1
starlette==1.6.0
sympy==1.14.0
tokenizers==0.23.1
torchcodec==0.16.0
tqdm==4.70.0
typer==0.27.1
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
